#include <windows.h>
#include <stdio.h>
#include "WolfDef.h"

playerstruct Player; // player struct (pos, health etc...)
/*
void Player_Reset(char NewGame)
{
	if(NewGame)
	{
		Player.health=START_HEALTH;
		Player.ammo=START_AMMO;
		Player.weapons=START_WEAPONS;
		Player.curweapon=START_WEAPON;
		Player.chsweapon=START_WEAPON;
		Player.lives=START_LIVES;
		Player.score=START_SCORE;
		Player.nextextra=EXTRAPOINTS;
	}
	Player.keys=START_KEYS;
	Player.movspd=0;
	Player.strspd=0;
	Player.rotspd=0;
	Player.rotchg=0;

	Player.weapframe=0;
	Player.fire=0;

	playstate=ex_stillplaying;
}
*/
